# fx_platform

FX bot + Backend API + Web UI (PostgreSQL)

## Структура
- `bot_fx/` — Telegram-бот курсов валют
- `backend/` — FastAPI сервис (API)
- `infra/` — docker-compose, миграции

## Быстрый старт
```bash
cd infra
docker-compose up -d

